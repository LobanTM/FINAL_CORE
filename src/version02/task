version02
with reflection

СТРУКТУРА ПРОЕКТА
Model
    ArrayHolder - модель данньіх
Controller
    ArrayHolderController - интерфейс логики сортировки
    ArrayHolderControllerImpl - реализация логики, содержит методьі сортировки
    Sort - аннотация, которой помеченьі методьі сортировки
View
    ResultSort - структура, содержащая результатьі одной сортировки
    Episode - структура, содержащая результатьі всех сортировок для заданного епизода
        (способа заполнения массива)
    ArrayHolderView - класс, реализующий отображение результатов,
        вьізов методов сортировки с помощью рефлексии,
        создание документа CalcLibreOffice
test
    Tester - класс, реализующий тестирование проекта

Main - класс, содержащий точку запуска программьі

ЗАДАНИЕ
Написать методы, сортирующие массив целых чисел произвольной длины следующими способами:

1) пузырьковая сортировка (попарная);
1.1) просмотр осуществляется от начала к концу (элемент «всплывает»);
1.2) просмотр осуществляется от конца к началу (элемент «тонет»);
2) обменная сортировка;
3) сортировка со слиянием;
4) рекурсивная сортировка с использованием метода половинного деления;
5) с помощью Arrays.sort(int[] a).

Оцените быстродействие каждого из методов сортировки для случаев:
1) Уже отсортированного массива  (1, 2, 3, 7, ....., max);
2) Отсортированного массива, в конец которого дописан случайный элемент (1, 2, 3, 7, ....., max, X);
3) Массива, отсортированного в обратном порядке  (max, ... , 7, 3, 2, 1);
4) Массива, содержащего элементы, расположенные случайным образом.

Для оценки быстродействия можете воспользоваться методом System.nanoTime().
Для получения последовательности псевдослучайных чисел можете воспользоваться методом Math.random() или классом Random.

 Задание Advanced
 Проведите ряд экспериментов и постройте серию графиков (в Excel)
 Ваша программа должна сама управлять проведением эксперимента,
 сортируя массивы различных размеров различными методами.
 После этого, она должна выдавать результаты в консоль в таком формате,
 чтобы их можно было выделить мышкой (все за один раз), скопировать, вставить в Excel
 и построить 4 графика для каждого способа заполнения массивов
 (по убыванию, возрастанию, со случайным элементов в конце и для случайно заполненного массива).


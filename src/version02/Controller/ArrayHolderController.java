package version02.Controller;

import version02.Model.ArrayHolder;

/**
 * Created by tetya on 21.07.2017.
 */
public interface ArrayHolderController {
    long bubbleSort(ArrayHolder arrayHolder);
    long exchangeSort(ArrayHolder arrayHolder);
    long mergeSort(ArrayHolder arrayHolder);
    long recursiveSort(ArrayHolder arrayHolder);
    long basicSort(ArrayHolder arrayHolder);
    long newSort(ArrayHolder arrayHolder);

}
